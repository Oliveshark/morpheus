#include "Box.h"
#include "../components/PhysicsComponent.h"

Box* Box::createBox(const SDL_Rect& box, SDL_Color color)
{
	(void) box;
	(void) color;

	// TODO(Linus): Complete this
	//auto *entity = new Box();
	//auto *physComp = new PhysicsComponent();
	//entity->addComponent(physComp);

	return nullptr;
}
